[pytest]
# Recursive discovery from the suite root (meta/ + flows/ market/ security/ resilience/ ops/).
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers --tb=short
timeout = 600
